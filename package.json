{
  "name": "ai-points-calculator-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd points-calculator && npm install && npm run build",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
